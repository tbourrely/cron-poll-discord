# cron-poll-discord

## Getting started

Create *.env* file with :

```
DISCORD_TOKEN=<discord bot token>
```

Run :

```
# bot
cargo run --bin bot

# api
cargo run --bin api

# sender
cargo run --bin sender
```

Run tests :

```
cargo test
```
